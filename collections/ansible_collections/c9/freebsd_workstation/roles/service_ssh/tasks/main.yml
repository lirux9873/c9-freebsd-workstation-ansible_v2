---
- name: "service_ssh | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role service_ssh is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: service_ssh_enabled | bool
  changed_when: false
