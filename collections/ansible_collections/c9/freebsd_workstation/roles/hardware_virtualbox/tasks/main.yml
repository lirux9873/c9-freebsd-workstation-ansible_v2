---
- name: "hardware_virtualbox | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role hardware_virtualbox is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: hardware_virtualbox_enabled | bool
  changed_when: false
