---
- name: "common_filesystem | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_filesystem is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_filesystem_enabled | bool
  changed_when: false
