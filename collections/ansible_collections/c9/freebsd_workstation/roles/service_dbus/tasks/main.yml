---
- name: "service_dbus | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role service_dbus is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: service_dbus_enabled | bool
  changed_when: false
