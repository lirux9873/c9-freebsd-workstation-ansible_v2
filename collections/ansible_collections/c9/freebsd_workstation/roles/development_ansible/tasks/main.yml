---
- name: "development_ansible | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role development_ansible is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: development_ansible_enabled | bool
  changed_when: false
