---
- name: "common_packages | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_packages is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_packages_enabled | bool
  changed_when: false
