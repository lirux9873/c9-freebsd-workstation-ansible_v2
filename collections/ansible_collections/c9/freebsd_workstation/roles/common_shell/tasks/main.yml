---
- name: "common_shell | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_shell is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_shell_enabled | bool
  changed_when: false
