---
- name: "common_validation | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_validation is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_validation_enabled | bool
  changed_when: false
