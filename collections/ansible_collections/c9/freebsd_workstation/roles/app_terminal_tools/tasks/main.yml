---
- name: "app_terminal_tools | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role app_terminal_tools is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: app_terminal_tools_enabled | bool
  changed_when: false
