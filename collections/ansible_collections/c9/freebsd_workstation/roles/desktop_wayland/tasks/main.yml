---
- name: "desktop_wayland | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role desktop_wayland is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: desktop_wayland_enabled | bool
  changed_when: false
