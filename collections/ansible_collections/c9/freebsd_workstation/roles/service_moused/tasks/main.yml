---
- name: "service_moused | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role service_moused is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: service_moused_enabled | bool
  changed_when: false
