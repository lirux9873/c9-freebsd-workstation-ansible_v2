---
- name: "development_base | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role development_base is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: development_base_enabled | bool
  changed_when: false
