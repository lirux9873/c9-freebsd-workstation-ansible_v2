---
- name: "common_sudo | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_sudo is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_sudo_enabled | bool
  changed_when: false
