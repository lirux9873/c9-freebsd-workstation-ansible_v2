---
- name: "networking_zeroconf | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role networking_zeroconf is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: networking_zeroconf_enabled | bool
  changed_when: false
