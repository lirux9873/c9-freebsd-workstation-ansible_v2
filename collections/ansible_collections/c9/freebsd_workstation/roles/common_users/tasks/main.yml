---
- name: "common_users | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role common_users is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: common_users_enabled | bool
  changed_when: false
