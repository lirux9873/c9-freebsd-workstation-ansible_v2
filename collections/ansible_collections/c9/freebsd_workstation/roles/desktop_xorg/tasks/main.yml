---
- name: "desktop_xorg | Placeholder"
  ansible.builtin.debug:
    msg: >-
      Role desktop_xorg is scaffolded but not implemented yet.
      Replace this task with the role implementation when the feature is enabled.
  when: desktop_xorg_enabled | bool
  changed_when: false
